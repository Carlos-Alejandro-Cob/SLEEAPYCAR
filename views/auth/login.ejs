<%- include('../layouts/header', { ...locals, title: 'Iniciar Sesión - SLEE APYCAR' }) %>

<div class="row justify-content-center mt-5">
    <div class="col-md-5">
        <div class="card shadow-sm">
            <div class="card-body p-4">
                <div class="text-center mb-4">
                    <i class="fas fa-truck fa-3x text-primary"></i>
                    <h1 class="h3 mt-3 mb-1 fw-normal">Iniciar Sesión</h1>
                    <p class="text-muted">Accede al panel de administración</p>
                </div>

                <form action="/auth/login" method="POST">
                    <div class="form-floating mb-3">
                        <input type="text" class="form-control" id="nombre_usuario" name="nombre_usuario" placeholder="Nombre de usuario" required autofocus>
                        <label for="nombre_usuario">Nombre de usuario</label>
                    </div>
                    <div class="form-floating mb-3">
                        <input type="password" class="form-control" id="password" name="password" placeholder="Contraseña" required>
                        <label for="password">Contraseña</label>
                    </div>

                    <button class="w-100 btn btn-lg btn-primary" type="submit">
                        <i class="fas fa-sign-in-alt me-2"></i>
                        Acceder
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>

<style>
    /* Estilos específicos para la página de login */
    body {
        background-color: #f8f9fa;
    }
    .navbar {
        display: none; /* Ocultar navbar en la página de login */
    }
    footer {
        display: none; /* Ocultar footer en la página de login */
    }
</style>

<%- include('../layouts/footer') %>