name: apycar_repartidor
description: Aplicación móvil para repartidores de APYCAR
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # UI
  cupertino_icons: ^1.0.6
  
  # HTTP y Networking
  http: ^1.1.0
  dio: ^5.4.0
  
  # Estado y almacenamiento
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  
  # Escáner de códigos
  mobile_scanner: ^5.2.3
  
  # Utilidades
  intl: ^0.19.0
  flutter_launcher_icons: ^0.13.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
